name: Build and Deploy
on: [push]
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    container: docker.pkg.github.com/icn-team/android-build/hicn:latest
    steps:
      - uses: actions/checkout@master
      - name: Build
        run: |
          ./build_apk.sh
